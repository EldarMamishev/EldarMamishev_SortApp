<UserControl x:Class="View.Views.SettingControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0" ItemsSource="{Binding SortAlgorithms.Values}" SelectedItem="{Binding SelectedSortAlgorithm, Mode=OneWayToSource}">
            <ListView.View>
                <GridView>
                    <GridViewColumn>Sorting algorithm</GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView Grid.Column="1" ItemsSource="{Binding SortTypes.Values}" SelectedItem="{Binding SelectedSortType, Mode=OneWayToSource}">
            <ListView.View>
                <GridView>
                    <GridViewColumn>Sort type</GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
