<UserControl x:Class="View.Views.InputControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="Input sequence:" MaxHeight="60"/>
        <TextBox Grid.Column="1" TextWrapping="Wrap" Text="{Binding InputSequence, Mode=TwoWay}" MinHeight="60" MinWidth="100" MaxHeight="100" MaxWidth="500"/>
    </Grid>
</UserControl>
